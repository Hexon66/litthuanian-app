"use client";

// Replaced with CSS noise texture in globals.css body::before
// This component is kept as a no-op for import compatibility
export default function FloatingBackground() {
  return null;
}
